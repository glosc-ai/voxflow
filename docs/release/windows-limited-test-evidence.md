# Windows 受限测试包验收记录

## 2026-07-28：应用程序控制策略阻断

### 制品

- 提交：`d171dac`
- 文件：`voxflow-windows-x64.zip`
- 大小：14,717,950 字节
- SHA-256：`f154e2c99ab1198408f69cda67920976d93373acdcd1a2b8f47c37b37e0e3c54`

### 环境摘要

- 主机：Microsoft Windows 10.0.26200.8875，Windows x64。
- 客体：全新 Windows Sandbox，不含 Flutter SDK、项目源码或开发机应用数据。
- 分发方式：将完整 Release 目录只读映射到客体 `C:\VoxFlow`。
- 沙盒配置：启用网络和音频输入，禁用剪贴板重定向。
- 运行时：压缩包包含 Flutter 引擎、插件、资源、SQLite 和应用本地部署的 x64 Visual C++ Runtime。

### 自动验证结果

- 通过 Windows Release 构建。
- 通过 ZIP SHA-256 校验。
- 通过解压后的必需文件和目录检查。
- 未发现 Dart 源码、keystore 或签名材料。
- 在开发机解压目录中通过进程启动存活检查。

### 沙盒结果

`C:\VoxFlow` 映射和运行时文件均可见。直接启动 `voxflow.exe` 时，系统在应用进入点前显示：

> 应用程序控制策略已阻止此文件。

这是客体操作系统的执行策略硬拦截，不是 VoxFlow 运行时崩溃，也不是可由测试人员确认后继续的 SmartScreen 警告。验收没有关闭、削弱或绕过系统安全策略。

由于应用进程未获准启动，以下工单 #6 条目未执行：数据与隐私说明、设置保存、模型获取、STT、TTS、历史记录、日志导出和重启持久化。

### 结论

当前结果为 **No-Go**，工单 #6 保持未完成。后续应在允许执行未签名受限测试程序的全新 Windows Sandbox 或隔离虚拟机中复测；另一条路径是引入受信任的 Windows 代码签名，但代码签名明确不属于首版范围。不得通过禁用或绕过应用程序控制策略来取得通过结果。
