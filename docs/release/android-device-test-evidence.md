# Android 签名 APK 真机验收记录

## 2026-07-28：缺少正式制品与真机

### 前置条件检查

- `flutter devices` 只发现 Windows、Chrome 和 Edge，没有 Android 真机。
- Android 组织签名工作流位于本地提交 `797c680`，当前分支没有推送。
- 当前 checkout 中的 `origin/main` 指向 `9685012`，不包含组织签名工作流。
- 本次刷新 GitHub 远程引用时网络连接失败，因此没有取得新的远程状态、正式 Actions 运行或制品链接。

### 未执行项目

由于没有来自受保护发布流程的组织签名候选 APK，也没有 Android 真机，以下工单 #7 项目均未执行：

- 安装并核对组织 Release 证书指纹。
- 麦克风权限、录音、暂停、继续和停止转录。
- 文件导入、转录及 TXT/SRT 导出。
- TTS 生成、播放、暂停、继续、拖动、音量调节和另存。
- 历史搜索、复制、重播和安全删除受管音频。
- 重启后的设置、历史记录和受管音频引用持久化。
- 数据与隐私说明、设置状态隔离和日志脱敏检查。

### 结论

当前结果为 **No-Go**，工单 #7 保持未完成。Debug APK、模拟器和一次性测试 keystore 不能替代本工单要求的组织签名制品与 Android 真机。

恢复验收需要同时满足：

1. 将经过审核的发布工作流推送到远程 `main`。
2. 由组织管理员配置受保护的 `android-release` Environment、正式签名 Secrets 和预期证书 SHA-256。
3. 从受保护流程生成各 ABI 候选 APK，并保留运行链接、制品链接和非敏感证书指纹。
4. 连接至少一台 Android 真机，再逐项执行本工单验收。
