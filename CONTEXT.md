# VoxFlow

VoxFlow 是面向 Windows 与 Android 的语音转文字和文字转语音效率工具。本文件记录项目讨论中已经确认的共同语言。

## 发布阶段

**首个上线版本**：
面向有限测试用户分发的 Windows 与 Android MVP，用于真实环境验证，不等同于公开商店正式版。
_Avoid_：公开发布版、正式商店版

**完成基线**：
已经由现有实现验证、在首个上线版本中只允许补缺和修复缺陷，不进行替换性重构的产品能力集合。
_Avoid_：待重构范围、可替换原型

**Android 真机验收**：
首个上线版本必须在至少一台 Android 真机上完成核心流程验证；只有模拟器或 APK 构建成功不视为通过。
_Avoid_：模拟器验收、仅构建验收

**组织发布签名**：
由 `glosc-ai` 组织持有、从首个上线版本起持续用于 Android 发布的唯一签名身份，其材料不进入源代码仓库。
_Avoid_：GitHub 自动签名、临时签名、Debug 签名

**受限测试密钥**：
受限测试用户专门用于 VoxFlow 验证、可随时撤销且额度受控的 API Key；不得使用生产或高价值长期密钥。
_Avoid_：生产密钥、长期主密钥

**平台安全凭据**：
API Key 在 Windows 由当前用户级 DPAPI 加密，在 Android 由 Android Keystore AES-GCM 加密；普通偏好、日志和错误提示不保存或显示密钥。
_Avoid_：SharedPreferences 明文密钥、硬编码加密密钥、日志中的认证信息

**完整数据重置**：
从设置页删除平台安全凭据、全部应用偏好、历史记录、受管音频、VoxFlow 临时音频和诊断日志，并重新显示数据与隐私说明；用户原始导入与另存文件始终排除。
_Avoid_：仅重置偏好、扫描或删除用户文件

**数据与隐私说明**：
受限测试用户在提供 API Key、文本或音频前必须看到的数据保存、外发、日志和删除行为说明。
_Avoid_：隐式同意、仅面向开发者的说明

**Windows 成品验收**：
最终分发文件必须在不含 Flutter 开发环境的全新 Windows 沙盒中通过核心流程和重启持久化验证。
_Avoid_：开发机验收、仅构建验收

**Windows 受限测试包**：
面向有限测试用户的未签名 Windows Release 压缩包，允许出现已提前说明的 SmartScreen 警告。
_Avoid_：正式安装包、公开发布包

**首版音色目录**：
首个上线版本中承诺可用且已经验证模型与资源匹配的音色集合，包括 OpenAI 六种标准音色和 `zh_female_cancan_uranus_bigtts`。
_Avoid_：服务商全部音色、未验证 Speaker ID

**首版测试门槛**：
格式检查、静态分析、现有自动测试与核心状态回归测试必须通过，并由 Windows 全新沙盒和 Android 真机冒烟补足平台交互验证。
_Avoid_：仅自动化验收、仅人工验收、全面自动化覆盖
