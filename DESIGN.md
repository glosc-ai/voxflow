# VoxFlow Design System

> 版本：v1.0  
> 状态：UI 全面重构的设计基线  
> 更新日期：2026-08-01  
> 适用平台：Windows Desktop、Android Mobile  
> 支持语言：简体中文、英文

## 1. 文档目的

本文档定义 VoxFlow UI 全面重构所使用的视觉语言、设计 Token、组件规范、交互状态、响应式布局、无障碍要求和平台差异。

它是后续设计与实现的单一基线。新界面必须从本规范派生，不应在功能页面中随意新增颜色、字号、间距、圆角或交互模式。

本次重构的目标不是把应用做得更“炫”，而是让它像一款可靠的桌面与移动端生产力工具：

- 可靠：状态清晰、错误可恢复、操作结果可预期。
- 专业：视觉克制、结构稳定、信息层级明确。
- 轻量：避免过度装饰、厚重阴影和不必要的动效。
- 高效：保持中高信息密度，缩短完成转录、合成和查找任务的路径。
- 包容：完整支持深色/浅色、键盘、大字体、中英文和屏幕阅读器。

## 2. 当前界面诊断

当前产品已经具备清晰的功能分区和可用的核心流程，但整体仍接近 Flutter Material 3 默认外观，存在以下问题：

1. 视觉层级主要依赖默认 Card，页面、区块和操作之间的主次关系不够明显。
2. 大屏下内容被置于宽卡片中央，部分区域留白过多，但关键设置仍集中在单列长表单中。
3. 主色、表面色和控件状态主要由 `ColorScheme.fromSeed` 自动生成，品牌识别和语义颜色缺乏统一约束。
4. 按钮、输入框、历史记录和播放器沿用原生组件组合，交互一致性不足。
5. 键盘焦点、快捷键、大字体重排和中英文长度差异尚未形成系统规则。
6. 成功、警告、错误、加载、空状态主要由各页面独立处理，反馈层级不统一。

重构应保留现有产品骨架：

- 四个一级功能：语音转文字、文字转语音、历史记录、设置。
- Windows 使用侧边导航，Android 使用底部导航。
- 内容区最大宽度约 920–960 px。
- 页面外边距 24 px、卡片内边距 20 px、主要区块间距 16 px 的现有节奏。
- 现有 Riverpod 状态和功能流程，不因为视觉重构而改变业务含义。

## 3. 设计原则

### 3.1 内容优先

音频状态、转录正文、待合成文字、模型设置和历史内容必须是页面的视觉主体。装饰元素不得抢夺注意力。

### 3.2 一屏一个主要动作

同一个任务区域最多有一个 Filled 主按钮。其他动作使用 Tonal、Outlined 或 Text 层级，避免多个按钮同时争夺注意力。

### 3.3 状态始终显式

录音、上传、转录、合成、播放、保存和失败均必须通过“图标 + 文字”表达。颜色和动画只能辅助，不能成为唯一信号。

### 3.4 克制的品牌表达

品牌以沉稳靛蓝、声波图形和清晰的排版建立识别。不使用霓虹、玻璃拟态、全息效果、扫描线、粒子、强渐变或游戏化成就反馈。

### 3.5 渐进披露

高频设置直接展示；技术解释、诊断详情和危险操作在需要时展开。不要用复杂页面一次呈现所有信息。

### 3.6 跨平台一致，交互遵循平台

视觉语义和任务流程保持一致；导航、尺寸、返回行为、权限和反馈方式遵循 Windows 与 Android 的平台习惯。

## 4. Design Tokens

### 4.1 色彩

现有品牌种子色 `#5268D8` 被保留并收敛为更稳重的靛蓝色系。所有颜色必须通过语义 Token 使用，功能页面不得直接引用原始色值。

#### 4.1.1 品牌与表面色

| Token | 浅色模式 | 深色模式 | 用途 |
|---|---:|---:|---|
| `color.primary` | `#455BB8` | `#B9C4FF` | 主按钮、选中导航、关键进度 |
| `color.onPrimary` | `#FFFFFF` | `#14245E` | 主色背景上的内容 |
| `color.primaryHover` | `#394DA3` | `#CAD2FF` | 主色控件悬停 |
| `color.primaryPressed` | `#2F418E` | `#AAB8F5` | 主色控件按下 |
| `color.primaryContainer` | `#E2E6FF` | `#2D3E87` | 选中项、品牌图标背景 |
| `color.onPrimaryContainer` | `#18265D` | `#E1E5FF` | 主色容器上的内容 |
| `color.canvas` | `#F7F8FC` | `#111318` | 页面底色 |
| `color.surface` | `#FFFFFF` | `#191C22` | 卡片、表单、对话框 |
| `color.surfaceSubtle` | `#F0F2F7` | `#21252D` | 次级分组、只读区域 |
| `color.surfaceRaised` | `#FFFFFF` | `#272B34` | 菜单、悬浮层 |
| `color.surfaceSelected` | `#EAEDFF` | `#293663` | 列表和导航选中状态 |
| `color.textPrimary` | `#191C20` | `#E5E8EF` | 标题、正文 |
| `color.textSecondary` | `#5C626D` | `#B9C0CC` | 说明、元数据、时间 |
| `color.textTertiary` | `#777E8A` | `#9098A5` | Placeholder、弱提示 |
| `color.border` | `#D9DEE8` | `#363C47` | 卡片、输入框边界 |
| `color.borderStrong` | `#AEB7C7` | `#56606E` | Hover、强调分隔 |
| `color.divider` | `#E5E8EF` | `#2D323B` | 区块分隔线 |
| `color.focus` | `#315CE8` | `#AFC0FF` | 键盘焦点环 |
| `color.scrim` | `#111318` 40% | `#000000` 64% | 模态遮罩 |

#### 4.1.2 语义颜色

| 语义 | 浅色前景 | 浅色背景 | 深色前景 | 深色背景 |
|---|---:|---:|---:|---:|
| `success` | `#18734A` | `#E5F4EB` | `#72D9A2` | `#123526` |
| `info` | `#2567A7` | `#E7F2FD` | `#8DC8FF` | `#123554` |
| `warning` | `#8A5C00` | `#FFF2D3` | `#F4C34F` | `#392B08` |
| `error` | `#B3261E` | `#FCE8E6` | `#FFB4AB` | `#591B18` |
| `recording` | `#B42318` | `#FEECEB` | `#FFB4AB` | `#591B18` |

#### 4.1.3 色彩使用规则

- 正文与背景的对比度必须至少达到 WCAG AA `4.5:1`。
- 大字、图标、边框和焦点指示至少达到 `3:1`。
- 深色模式不使用纯黑作为大面积背景，也不使用纯白作为大面积文字。
- 状态颜色必须与文字或图标共同出现。
- 录音红色只用于真实录音状态、停止操作和相关警告，不作为装饰色。
- 不允许以降低整个组件透明度的方式处理禁用状态，应分别定义禁用背景、边框和文字。
- 不使用大面积品牌色页面背景；品牌色主要出现在操作、选中态和小面积强调中。

### 4.2 字体

不新增字体包，使用平台系统字体，以确保中英文渲染质量、启动性能和平台一致性。

#### 4.2.1 字体栈

- Windows：`Segoe UI Variable` → `Segoe UI` → `Microsoft YaHei UI`
- Android：`Roboto` → `Noto Sans CJK SC`
- 日志、计时和技术标识：平台等宽字体

#### 4.2.2 字体层级

| Token | 字号 / 行高 | 字重 | 用途 |
|---|---:|---:|---|
| `type.pageTitle` | `24 / 32` | 600 | 一级页面标题 |
| `type.dialogTitle` | `20 / 28` | 600 | 对话框标题 |
| `type.sectionTitle` | `18 / 26` | 600 | 卡片和区块标题 |
| `type.titleSmall` | `16 / 24` | 600 | 列表标题、字段组标题 |
| `type.body` | `14 / 22` | 400 | 主要正文 |
| `type.bodyStrong` | `14 / 22` | 600 | 正文强调 |
| `type.bodySmall` | `12 / 18` | 400 | 辅助说明、时间、字符数 |
| `type.label` | `13 / 20` | 500 | 按钮、标签、导航 |
| `type.numeric` | `24 / 32` | 500 | 录音计时、播放时间 |
| `type.code` | `13 / 20` | 400 | 日志、API 路径、模型 ID |

#### 4.2.3 排版规则

- 中文正文不额外增加字间距；英文可使用 `0–0.1 px` 的轻微字距。
- 英文界面使用 Sentence case，不使用全大写标题。
- 标题默认最多两行；正文不得因固定高度被裁切。
- 计时使用等宽数字，防止数字变化导致布局跳动。
- 模型 ID、API Root 和文件名属于技术内容，可使用等宽字体，但不应牺牲可读性。
- 可点击文字不能仅通过颜色区分，还必须有按钮形态、下划线或明确上下文。

### 4.3 图标

- 使用项目已有的 Material Icons，避免增加新的图标依赖。
- 常规图标尺寸：`20 px`；导航和强调图标：`24 px`；空状态图标：`32 px`。
- 图标按钮必须提供 Tooltip 和语义标签。
- 同一个动作全局使用同一个图标，例如下载、复制、删除、播放不得在不同页面替换含义。
- 主要动作应使用“图标 + 文字”，只有高度熟悉且空间受限的操作才允许纯图标。
- 品牌声波图标保持静态和几何简洁，不加入发光、渐变或持续跳动。

### 4.4 间距

基础单位为 `4 px`。

| Token | 值 | 典型用途 |
|---|---:|---|
| `space.1` | `4` | 标题与短说明 |
| `space.2` | `8` | 图标与文字、紧密元素 |
| `space.3` | `12` | 按钮组、紧密字段 |
| `space.4` | `16` | 常规组件间距 |
| `space.5` | `20` | 桌面卡片内边距 |
| `space.6` | `24` | 页面内区块、页面边距 |
| `space.8` | `32` | 大区块分隔 |
| `space.10` | `40` | 空状态和强调区域 |
| `space.12` | `48` | 页面级大间隔 |

规则：

- Windows 页面边距使用 `24–32 px`，Android 使用 `16 px`。
- Windows 卡片内边距使用 `20 px`，Android 使用 `16 px`。
- 同组字段使用 `12–16 px`，不同分组使用 `24–32 px`。
- 不以空白替代清晰的标题和分组关系。

### 4.5 尺寸、圆角与阴影

| Token | 值 | 用途 |
|---|---:|---|
| `size.controlDesktop` | `40` | Windows 按钮、单行输入框最小高度 |
| `size.controlTouch` | `48` | Android 触控控件最小尺寸 |
| `size.iconTargetDesktop` | `40` | Windows 图标按钮点击区 |
| `size.iconTargetTouch` | `48` | Android 图标按钮点击区 |
| `radius.small` | `6` | 状态标签、小容器 |
| `radius.medium` | `8` | 按钮、输入框、菜单项 |
| `radius.large` | `10` | 卡片、Banner |
| `radius.dialog` | `12` | 对话框、底部面板 |
| `radius.full` | `999` | 圆形按钮、真正的胶囊标签 |

阴影规则：

- 页面卡片默认无阴影，使用表面色和 `1 px` 边框区分。
- 菜单、对话框和浮动播放器可使用单层轻阴影。
- 禁止多层大范围阴影和带品牌色的光晕。
- Hover 不抬升普通卡片；可交互列表通过背景和边框变化反馈。

## 5. 响应式布局

### 5.1 断点

| 模式 | 宽度 | 典型设备 |
|---|---:|---|
| Compact | `< 720 px` | Android 手机、窄窗口 |
| Medium | `720–1039 px` | 小型桌面窗口、平板 |
| Expanded | `1040–1439 px` | 标准 Windows 窗口 |
| Large | `≥ 1440 px` | 大屏桌面 |

现有 `720 px` 导航断点应保留。`1040 px` 用于决定 Navigation Rail 是否显示文字。

### 5.2 应用框架

#### Windows

- Medium：`72 px` 紧凑 Navigation Rail，仅显示图标，悬停展示 Tooltip。
- Expanded / Large：`232 px` 展开 Navigation Rail，显示图标和文字。
- 主内容区包含页面标题栏和滚动内容，最大宽度 `960 px`。
- 设置、隐私和其他单列表单最大宽度 `720 px`。
- Large 模式下内容保持居中，不把表单无限拉宽；可将辅助说明放入右侧信息栏。

#### Android

- 使用固定四项 Bottom Navigation，避免汉堡菜单隐藏一级功能。
- 页面采用 SafeArea，内容边距 `16 px`。
- 主要操作应处于单手可达位置；长页面可使用底部粘性操作区。
- 软键盘出现后，聚焦字段和主要提交按钮必须仍可访问。

### 5.3 大字体重排

- 支持系统文字缩放至 `200%`，不得强制限制 `textScaleFactor`。
- `≥ 130%`：按钮组允许换行；历史记录时间移至下一行。
- `≥ 160%`：所有“标签 + 控件”横向布局改为纵向布局。
- `≥ 200%`：正文和按钮标签不得裁切；页面可以纵向增长。
- 除日志、模型 ID 和表格型技术内容外，不允许页面级水平滚动。
- 断点判断应结合可用宽度和文字缩放，不能只依赖设备物理宽度。

## 6. 应用框架与导航

### 6.1 Navigation Rail

- 顶部展示静态 VoxFlow 声波标志。
- 导航顺序固定：语音转文字、文字转语音、历史记录、设置。
- 选中项使用 `surfaceSelected` 背景、`primary` 图标和文字，并提供明确的左侧或内部选中标记。
- Hover、Pressed、Focused 和 Selected 必须可以同时区分。
- 紧凑模式的 Tooltip 中显示名称和可用快捷键。

### 6.2 Bottom Navigation

- 四项均显示图标和短标签。
- 中英文标签必须在常规字体和 `130%` 缩放下保持完整。
- 不显示徽章，除非未来存在真实且需要处理的后台任务。
- 选中状态使用颜色与形状共同表达。

### 6.3 页面标题栏

- 左侧：页面标题；必要时增加一句简短说明。
- 右侧：页面级动作，如刷新、清空当前任务。
- 图标动作必须带 Tooltip；高风险动作不得只显示图标。
- Android 上保持 `56 px` App Bar；Windows 内容标题栏建议 `64 px`。

## 7. 核心组件

### 7.1 Card / Section

Card 是信息分组容器，不是默认布局工具。

- 普通 Card：`surface`、`1 px border`、`10 px radius`、无阴影。
- 强调 Card：使用 `primaryContainer` 或语义背景，但面积应受控。
- 可交互 Card：整卡有明确 Hover、Focus 和 Pressed 状态。
- 标题区、内容区和操作区之间使用间距或 Divider。
- 禁止 Card 套 Card；子分组使用 `surfaceSubtle` 或 Divider。

### 7.2 Button

#### 层级

- Filled：任务的唯一主要动作。
- Filled Tonal：重要次级动作，例如导出带时间戳字幕。
- Outlined：选择文件、获取模型、测试连接。
- Text：列表内播放、复制、关闭。
- Destructive：删除和清除，使用错误语义色。

#### 行为

- 文案采用“动词 + 对象”：`开始录音 / Start recording`。
- Loading 时保留按钮原宽度，显示 `16–18 px` 进度环和动作文字。
- 禁用时若原因不明显，应显示帮助文字或 Tooltip。
- Android 主按钮可全宽；Windows 主按钮通常按内容宽度，任务型大操作除外。
- 按下时使用颜色叠层，不缩放按钮。

### 7.3 Icon Button

- 点击区域满足平台最小尺寸，视觉图标为 `20–24 px`。
- 必须具有 Tooltip、语义标签和焦点环。
- 删除等危险操作使用错误色，但不能仅依赖垃圾桶图标表达后果。
- 同一工具栏中的图标按钮保持统一尺寸和间距。

### 7.4 Text Field

- Label 常驻，不使用 Placeholder 代替字段名称。
- Placeholder 只描述格式或示例。
- 帮助文字位于字段下方；错误出现时就地替换帮助文字。
- 多行字段支持字符数、清空和滚动，文本不得被固定高度裁切。
- 字符计数正常使用 `textSecondary`，达到 90% 限制后使用 `warning`。
- 密钥字段默认遮罩；显示/隐藏按钮拥有明确语义。
- 错误后保留用户输入，不自动清空。

### 7.5 Select / Dropdown

- 使用常驻 Label。
- 选项文本过长时允许菜单变宽或换行，字段内可单行省略。
- 聚焦或悬停后可以查看完整模型 ID。
- 必须支持键盘打开、上下移动、Enter 选择、Esc 关闭。
- 禁用下拉框时就近说明“正在请求”“服务未提供模型”等原因。

### 7.6 Slider

- 始终显示当前值，例如 `1.00×`、`80%`。
- 支持方向键细调，Page Up / Page Down 大步调整。
- 轨道颜色必须在深浅模式下可辨识。
- 语速建议提供 `0.5× / 1.0× / 1.5× / 2.0×` 等可理解的刻度语义。
- 音量为零时同时更新图标、数值和屏幕阅读器文本。

### 7.7 Banner

- 页面级错误、持续警告和隐私提示使用 Inline Banner。
- 结构：语义图标、简短标题、说明、可选动作。
- 错误标题回答“发生了什么”，说明回答“如何恢复”。
- Banner 可被聚焦，重试按钮位于内容之后。
- 关键错误不能只用 Snackbar 表达。

### 7.8 Snackbar

- 用于复制、保存、导出等短暂成功反馈。
- 默认展示 `4–6 秒`，包含必要时的撤销动作。
- 同一时刻只显示一条，重复消息应合并。
- Android 位于 Bottom Navigation 上方；Windows 位于内容区底部。

### 7.9 Dialog

- 普通桌面对话框宽度 `480–560 px`；日志对话框最大 `800 px`。
- 移动端随屏幕宽度，保留 `16 px` 外边距。
- 默认焦点落在安全操作。
- 破坏性按钮放在操作区末尾，但不得成为默认回车动作。
- Esc / Back 关闭非阻塞对话框。
- 长内容必须有独立滚动区域，标题和操作保持可见。

### 7.10 Progress

- 上传存在准确百分比时使用 Determinate Linear Progress。
- 无法估算的转录和合成使用 Indeterminate Progress，并同时显示阶段文字。
- 不伪造完成百分比。
- 超过 10 秒的任务应显示已用时间或取消入口（如果业务允许取消）。
- 进度结束后切换到明确的成功或失败状态。

### 7.11 Empty State

- 使用 `32 px` 线性图标、标题、说明和一个主要下一步。
- 文案必须解释为什么为空，而不是只写“暂无数据”。
- 不使用大型插画、吉祥物或游戏化文案。

## 8. 统一交互状态

| 状态 | 视觉 | 行为 |
|---|---|---|
| Default | 基础表面和边框 | 可正常操作 |
| Hover | 约 4% 色彩叠层，边框增强 | 仅鼠标设备 |
| Pressed | 约 8% 色彩叠层 | 不缩放组件 |
| Focused | `2 px` 焦点环，外偏移 `2 px` | 优先级高于 Hover |
| Selected | 主色容器、主色内容、形状标记 | 保持可辨识 |
| Disabled | 独立禁用背景、边框和文字 | 不响应输入，必要时解释原因 |
| Loading | 保留原布局和动作名称 | 阻止重复提交，不冻结无关区域 |
| Success | 成功图标和完整文字 | 明确下一步或自动回到稳定状态 |
| Warning | 警告色 Banner 或字段提示 | 给出风险与处理建议 |
| Error | 就近错误和恢复动作 | 保留用户数据，焦点移至错误摘要或首个错误字段 |
| Empty | 空状态说明和主要下一步 | 不呈现无意义禁用控件 |
| Offline | 网络状态说明 | 区分断网、认证失败和服务端失败 |
| Recording | 静态红点、状态文字、计时 | 停止控制持续可见 |

## 9. 页面重构蓝图

### 9.1 数据与隐私说明

目标：让用户在首次使用前快速理解数据去向，而不是阅读一张长卡片。

#### Windows

- 居中内容，最大宽度 `720 px`。
- 标题下提供一句摘要：“使用前，请确认 API Key、音频、文本和日志的处理方式。”
- 四项说明使用无嵌套的列表区块：图标、标题、两行说明。
- 对高风险信息“API Key 以明文保存在本机”使用 Warning Banner。
- 底部固定主要按钮“我已了解并继续”。

#### Android

- 内容全宽滚动，底部使用 SafeArea 粘性操作区。
- 大字体时操作按钮不遮挡正文。

### 9.2 语音转文字（STT）

目标：突出“录制或导入 → 处理 → 编辑 → 导出”的线性任务流。

#### 待机状态

1. 顶部任务区：
   - 页面标题和简短说明。
   - 一个紧凑录音面板，包含麦克风图标、状态、计时和主按钮。
2. 导入区：
   - 与录音并列或紧邻，显示支持格式、限制和选择文件按钮。
   - 文件选中后显示文件名、大小、格式和移除动作。
3. 不保留大面积纯装饰留白。

#### 录音状态

- 状态文本改为“正在录音”，配静态红点和等宽计时。
- 暂停为 Outlined，停止并转录为主操作。
- 不使用持续放大的麦克风或声波动画。
- 离开页面前若录音仍在进行，应弹出明确确认。

#### 处理状态

- 在原任务区内切换到上传/转录进度，不额外叠加新的孤立 Card。
- 显示当前阶段：准备音频、上传、转录、整理结果。
- 网络错误保留已选择的文件或录音，以便重试。

#### 结果状态

- 结果编辑器成为页面主体，桌面最小可见高度建议 `320 px`。
- 标题行显示“转录结果”、片段数和可选时长。
- 导出 TXT、导出 SRT 放在编辑器操作栏右侧；Android 放在底部操作区。
- SRT 不可用时说明缺少时间戳，而不是只禁用按钮。
- “新建转录”放在页面标题栏，替代含义模糊的刷新图标。

### 9.3 文字转语音（TTS）

目标：让文字编辑、声音配置和试听形成紧凑的创作工作区。

#### Windows Expanded

- 使用主次双栏：
  - 左侧约 60%：文本编辑器和字符计数。
  - 右侧约 40%：音色、语速和合成按钮。
- 生成成功后，播放器作为底部跨栏区域或粘性轻量面板出现。
- 双栏在小于 `1040 px` 或大字体模式下自动变回单列。

#### Android / Compact

- 顺序为文字 → 音色 → 语速 → 合成 → 播放器。
- 合成按钮全宽并靠近参数区底部。
- 播放器在内容流中展示，不遮挡系统导航和软键盘。

#### 文本编辑器

- 保持 4096 字符限制。
- 达到 90% 时字符数进入警告状态。
- 生成期间可以锁定提交，但不应阻止复制现有文字。

#### 参数区

- 将技术性 Speaker ID 显示为辅助信息；字段主名称统一为“音色 / Voice”。
- 语速值始终可见，并提供键盘细调。
- 合成按钮文案在处理中保持“正在合成…”。

#### 播放器

- 第一行：合成音频标题、保存 MP3。
- 第二行：播放/暂停、进度、当前时间/总时长。
- 第三行：音量和数值；移动端可简化为音量图标与滑块。
- 播放按钮是播放器内部主控制，但不能与页面“合成语音”争夺同级视觉权重。

### 9.4 历史记录

目标：提高浏览和检索密度，减少每条记录的卡片感。

#### 顶部工具区

- 搜索框占据主要宽度，支持 `Ctrl+F`。
- 刷新动作带 Tooltip；未来过滤器可放在搜索框之后。
- 搜索状态显示结果数量，而不是静默过滤。

#### 记录列表

- Windows 优先使用紧凑列表行或弱分隔 Card，不使用厚重独立大卡片。
- 信息顺序：类型 → 摘要 → 时间 → 操作。
- 摘要默认两行；大字体不限制行数。
- STT/TTS 类型用图标和文字共同表达。
- 播放、复制为 Text 动作；删除在行尾并使用错误语义。
- 记录多时使用渐进加载，不在首屏渲染全部长文本。

#### 删除

- 确认对话框必须说明是否同步删除受管音频。
- 删除成功使用 Snackbar，并在业务可行时提供撤销。
- 删除失败时恢复该行并显示就近错误。

#### 空状态与错误

- 无记录：“还没有历史记录。完成一次转录或语音合成后会显示在这里。”
- 无搜索结果：“没有找到包含‘…’的记录。”并提供清除搜索动作。
- 加载失败：显示 Inline Banner 和重试按钮，已有缓存记录仍应可见。

### 9.5 设置

目标：将高频连接配置、模型选择和低频诊断功能清晰分组。

#### 信息架构

1. API 连接
   - API Root
   - API Key
   - 获取模型
   - 测试连接
2. 模型
   - 语音转文字模型
   - 文字转语音模型
3. 外观与体验（实施主题切换时）
   - 跟随系统 / 浅色 / 深色
   - 可选紧凑密度，仅 Windows
4. 诊断日志
   - 数据范围说明
   - 查看日志
   - 导出日志

#### 布局

- 最大宽度 `720 px`；Large 模式可将诊断说明放入右侧辅助栏。
- 每一组使用 Section Header 和 Divider，不把所有内容放进一个超长 Card。
- “保存设置”是唯一 Filled 主按钮。
- “测试连接”是 Outlined 动作；成功后在连接组内显示持久成功状态。
- 获取模型的加载状态只锁定相关字段和动作，不锁定整页。
- 日志操作视觉权重低于连接配置。

#### 安全表达

- API Key 永远默认遮罩。
- 不在 Tooltip、Snackbar、日志或错误信息中显示密钥。
- 明文保存风险使用 Warning Banner，不使用普通灰色辅助文字弱化风险。

## 10. 键盘规范

### 10.1 全局快捷键

| 快捷键 | 动作 |
|---|---|
| `Ctrl+1` | 语音转文字 |
| `Ctrl+2` | 文字转语音 |
| `Ctrl+3` | 历史记录 |
| `Ctrl+4` | 设置 |
| `Ctrl+F` | 聚焦当前页面搜索，适用于历史记录 |
| `Ctrl+O` | STT 页面选择文件 |
| `Ctrl+Enter` | 执行当前页面主要提交动作 |
| `Esc` | 关闭菜单、对话框或取消临时状态 |

`Ctrl+Enter` 必须根据焦点上下文执行：

- STT 待机且没有录音时，不应意外启动麦克风；录音必须由明确按钮触发。
- TTS 文本编辑器中执行“合成语音”。
- 设置表单中执行“保存设置”。

### 10.2 标准键盘行为

- `Tab / Shift+Tab`：按视觉顺序移动焦点。
- `Enter / Space`：激活按钮和选中项。
- 方向键：导航项、下拉项、Slider 和播放器进度。
- `Home / End`：列表和 Slider 移至边界。
- `Space`：播放器获得焦点时播放或暂停。
- 焦点进入滚动区域时必须自动滚入可见范围。
- 模态对话框必须限制焦点在其内部，关闭后焦点回到触发控件。

## 11. 无障碍

### 11.1 视觉

- 满足 WCAG 2.1 AA 对比度。
- 焦点环在所有背景上可见，不能被组件裁切。
- 所有状态都使用颜色以外的第二种表达方式。
- 不使用闪烁内容；任何亮度闪烁不得超过安全阈值。
- 大字体下不裁切正文、标签、按钮和导航文字。

### 11.2 屏幕阅读器

- 图标按钮提供动作名称，例如“显示 API Key”，而不是“眼睛图标”。
- 录音状态变化播报一次；计时器不得每秒触发播报。
- 进度阶段变化时播报“正在上传”“正在转录”等有意义信息。
- 错误发生后先播报错误摘要，再允许用户导航到错误字段。
- Slider 读出名称、当前值、单位和可调范围。
- 历史记录整行具有可理解的组合语义，同时保留内部按钮操作。

### 11.3 触控与运动

- Android 可触控区域至少 `48 × 48 dp`。
- Windows 图标按钮点击区至少 `40 × 40 px`。
- 遵循系统 Reduce Motion 设置。
- 不要求用户完成复杂拖拽；Slider 也必须支持点击和键盘调整。

## 12. 国际化与文案

### 12.1 布局

- 为英文预留至少 30% 文案膨胀空间。
- 不通过缩小字号适应英文按钮，应优先扩宽、换行或调整布局。
- 不为中文标签设置仅适合固定字数的宽度。
- 日期、时间、数字和文件大小使用系统区域格式。
- 模型 ID、API Root、文件扩展名和 Speaker ID 保留原文。

### 12.2 命名

| 中文 | 英文 |
|---|---|
| 语音转文字 | Speech to text |
| 文字转语音 | Text to speech |
| 历史记录 | History |
| 设置 | Settings |
| 音色 | Voice |
| 语速 | Speed |
| 转录结果 | Transcript |
| 开始录音 | Start recording |
| 停止并转录 | Stop and transcribe |
| 合成语音 | Generate speech |

同一界面不得混用“音色”“Voice”“Speaker ID”作为同级名称。推荐用“音色 / Voice”作为字段名，仅在辅助说明中保留 Speaker ID。

### 12.3 文案语气

- 简洁、直接、尊重用户，不使用拟人化或游戏化表达。
- 成功消息说明已完成的事实：“MP3 已保存。”
- 错误消息包含原因和恢复动作：“无法连接 API。请检查地址和网络后重试。”
- 避免“糟糕”“太棒了”“解锁”等情绪化或游戏化词语。

## 13. 动画与反馈

### 13.1 时长

| 类型 | 时长 |
|---|---:|
| 按压、即时反馈 | `80–120 ms` |
| Hover、焦点、颜色切换 | `120–160 ms` |
| 页面内状态切换 | `160–200 ms` |
| 对话框、底部面板 | `200–240 ms` |

### 13.2 缓动

- 进入：`easeOutCubic`
- 退出：`easeInCubic`
- 状态切换：标准 ease-in-out

### 13.3 限制

- 不使用弹跳、弹簧、视差、粒子、扫描线、霓虹光晕和大面积渐变。
- 不通过按钮缩放表达按压。
- 除真实进度外，避免持续循环动画。
- 录音状态可以使用非常轻微的状态点呼吸效果，周期不得短于 `1200 ms`。
- 开启 Reduce Motion 时取消位移、缩放和呼吸动画；必要反馈使用不超过 `80 ms` 的淡入淡出。

## 14. 平台差异

| 项目 | Windows | Android |
|---|---|---|
| 主导航 | Navigation Rail | Bottom Navigation |
| 主要输入 | 鼠标、键盘 | 触控、软键盘 |
| 控件最小尺寸 | `40 px` | `48 dp` |
| 页面边距 | `24–32 px` | `16 px` |
| Hover | 支持 | 不适用 |
| 焦点环 | 始终完整支持 | 外接键盘时显示 |
| 操作布局 | 可同行并右对齐 | 主要动作可全宽或底部粘性 |
| 文件导入 | 系统文件选择器 | Storage Access Framework |
| 麦克风权限 | 不显示运行时权限请求 | 首次使用时请求并解释用途 |
| 返回行为 | Esc 关闭临时层 | 系统 Back 先关闭临时层 |
| 滚动条 | 滚动或 Hover 时可见 | 使用平台滚动指示 |
| 反馈 | Tooltip、快捷键提示 | Snackbar、适度触觉反馈 |
| 页面过渡 | 极短或无 | 遵循 Material 平台过渡 |

Android 权限请求只在真正开始录音前出现；Windows 必须绕过 `permission_handler` 的运行时权限提示。

## 15. 主题与组件实现边界

后续实现必须集中管理设计 Token，避免将视觉常量散落到 Feature 页面。

建议职责划分：

- `core/theme/app_theme.dart`：组装 `ThemeData`、浅色/深色主题。
- `core/theme/app_colors.dart`：品牌与语义颜色 Token。
- `core/theme/app_typography.dart`：文字层级。
- `core/theme/app_spacing.dart`：间距、尺寸、圆角常量。
- `core/theme/app_theme_extensions.dart`：Material `ColorScheme` 未覆盖的 recording、warning 等语义 Token。
- `widgets/`：真正跨 Feature 复用的按钮、Section、Banner、Empty State、播放器原语。
- `features/*/widgets/`：仅服务单一 Feature 的组合组件。

实现规则：

- Feature 页面不得直接使用 `Color(0x...)`、`Colors.*` 或随意新增 `EdgeInsets` 常量。
- 优先通过 Material 组件主题统一按钮、输入框、Card、Dialog、Slider 和 Navigation。
- 全局异步状态继续使用 Riverpod；视觉重构不得将复杂页面状态迁回 `setState`。
- 不新增 `pubspec.yaml` 之外的依赖，除非另行评审。
- 主题模式属于全局设置，应由 Riverpod 和现有设置存储管理。
- 保留 Feature-First 架构，不为了样式复用制造跨 Feature 业务耦合。

## 16. 重构阶段

### Phase 1：设计基础

- 建立颜色、排版、间距、圆角、阴影和语义状态 Token。
- 完成 Light、Dark、System 三种主题模式。
- 为 Material 组件建立统一 Theme。
- 添加主题和对比度基础测试。

### Phase 2：通用组件

- 实现 App Section、Inline Banner、Empty State、Loading Button、统一 Dialog。
- 统一输入框、下拉框、Slider、Snackbar 和焦点环。
- 建立组件的 Default、Hover、Pressed、Focused、Disabled、Loading、Error 状态。

### Phase 3：应用框架

- 重构 Navigation Rail、Bottom Navigation 和页面标题栏。
- 实现 Compact、Medium、Expanded、Large 响应式布局。
- 加入全局键盘导航与快捷键。

### Phase 4：核心页面

建议顺序：

1. 设置：验证表单、错误、成功、加载和深浅色组件。
2. TTS：验证编辑器、Slider、双栏和播放器。
3. STT：验证录音生命周期、进度和结果编辑器。
4. 历史记录：验证高密度列表、搜索、空状态和删除。
5. 隐私说明：验证首次启动、大字体和移动端粘性操作。

### Phase 5：无障碍与平台适配

- 验证 100%、130%、160%、200% 字体缩放。
- 完成 Windows 纯键盘流程。
- 验证 Android TalkBack、触控尺寸、软键盘和返回行为。
- 验证浅色、深色、高对比场景和 Reduce Motion。

### Phase 6：回归与收尾

- 保留重构前后关键页面截图进行视觉对比。
- 为稳定组件和关键状态增加 Widget / Golden Tests。
- 在 Windows 干净环境和 Android 真机完成核心流程验收。
- 删除旧的重复样式和不再使用的临时组件。

## 17. 验收标准

### 17.1 视觉

- 所有功能页面只使用本文定义的颜色、字体、间距和圆角层级。
- 深色与浅色模式均不存在低对比度、不可辨识边框或刺眼大面积纯色。
- 页面不再呈现明显的 Flutter 默认组件拼装感。
- 大屏空间利用合理，不出现无意义的大面积留白或被拉伸的表单。
- 不包含游戏化、科幻化、霓虹或玻璃拟态表现。

### 17.2 交互

- 所有异步任务具备 Loading、Success、Error 和 Retry 状态。
- 失败不会清除用户已经输入的文字、已选择文件或可复用的录音。
- 所有危险操作均说明后果并要求明确确认。
- 键盘焦点可见，Tab 顺序符合视觉顺序。
- Windows 核心流程可以不使用鼠标完成，录音启动除外仍须明确激活。

### 17.3 响应式与国际化

- 支持从窄屏 Android 到 Large Windows 窗口。
- 中文和英文在所有断点下不溢出、不裁切。
- 200% 字体缩放下所有核心流程仍可完成。
- 除技术内容外不出现页面级水平滚动。

### 17.4 平台

- Windows 不请求 Android 式运行时麦克风或存储权限。
- Android 触控目标至少为 `48 dp`，并正确处理 SafeArea 和软键盘。
- Android 系统 Back、Windows Esc 和各自导航行为符合平台预期。

### 17.5 工程质量

- 全局状态仍由 Riverpod 管理。
- 网络和 I/O 错误均映射为用户可理解的界面状态。
- 日志和界面反馈不泄露 API Key、认证头、音频或输入正文。
- `flutter analyze`、现有测试和新增主题/组件测试通过。
- Windows 与 Android 构建流程保持兼容。

## 18. 非目标

本轮 UI 重构不包含：

- 改变 STT、TTS 服务协议或模型能力。
- 引入社交、积分、等级、成就或其他游戏化系统。
- 引入 3D、动态图形背景或复杂音频可视化。
- 为追求视觉效果新增重量级动画、字体或组件依赖。
- 在没有产品需求支持的情况下重写业务状态和存储层。

---

本规范生效后，任何新增页面或组件都应优先复用既有 Token 和组件。若确实需要新增视觉模式，应先更新本文件并说明适用场景，再进入实现。
